function test() {
/*
  const data = VizzyCache.fetch(UrlFetchApp.fetch) 
  console.log(new Date().getTime() - data.timestamp)
  console.log(['shaxs', 'files', 'repos', 'owners'].map(k=>{
    return `${k}:${data.gd[k].size}`
  }))
  */
}
